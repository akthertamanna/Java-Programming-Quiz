package com.javaprogramming.quiz.interfaces;

public interface OnInsertCompleteListener {
    // Method that will be called when insert operation completes
    void onInsertComplete(boolean isSuccess);
}